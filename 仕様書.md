

---

# 記事保存・AI診断アプリ 仕様書

**プロダクト名（仮）**: `Veritas` — Save & Verify
**バージョン**: 1.0（初版）
**対象プラットフォーム**: iOS 17.0 以降（iPhone / iPad）

---

## 1. プロダクト概要

### 1.1 コンセプト
iPhone の**共有シート（Share Sheet）**から本アプリを選択するだけで、Web 記事を保存し、AI が自動的に以下 4 点を診断・生成するパーソナル情報ライブラリ。

| 診断項目 | 出力 |
|---|---|
| 保存（アーカイブ） | 本文抽出・リーダー表示・オフライン閲覧 |
| 信頼度診断 | 0〜100 のスコア + 根拠の内訳 + 警告フラグ |
| 要約 | 3行要約 / 箇条書き要約 / 詳細要約の3段階 |
| 情報の新しさ診断 | 鮮度スコア + 「陳腐化リスク」判定 + 後続情報の提示 |

### 1.2 解決する課題
- 「あとで読む」に溜めた記事が読まれず、価値が判断できない
- SNS 経由の記事の信頼性を人手で検証するコストが高い
- 数年前の技術記事・制度解説を最新情報と誤認して参照してしまう

### 1.3 ターゲットユーザー
1. 情報収集量の多いビジネスパーソン・リサーチャー
2. 学生・研究者（レポート作成時の一次情報確認）
3. エンジニア（技術情報の鮮度が致命的に重要な層）

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| Item | 保存された1件の記事レコード |
| Extraction | URL から本文・メタデータを抽出する処理 |
| Credibility Score | 信頼度スコア（0〜100） |
| Freshness Score | 情報鮮度スコア（0〜100） |
| Half-life | トピック分野ごとの情報半減期（日数） |
| Corroboration | 他ソースとの照合（裏取り）処理 |
| Successor | 当該記事より新しい後続情報 |

---

## 3. ユースケースフロー

### 3.1 メインフロー（共有からの保存）

```
Safari で記事を閲覧
  ↓
共有ボタン → 「Veritas に保存」を選択
  ↓
Share Extension が起動（0.3秒以内に UI 表示）
  ├─ URL / タイトル / ページ HTML を取得
  ├─ タグ・メモを任意入力（スキップ可）
  └─「保存」タップ
  ↓
App Group 経由でローカル DB に即時保存（オフラインでも成功）
  ↓
Background Task でサーバへ送信（キュー方式・リトライあり）
  ↓
サーバ側パイプライン実行（Extraction → 3種診断）
  ↓
完了時にプッシュ通知「診断が完了しました（信頼度 72 / 鮮度 41）」
  ↓
アプリ内ライブラリで結果閲覧
```

### 3.2 例外フロー

| ケース | 挙動 |
|---|---|
| 完全オフライン | ローカル保存のみ成功。ステータス `pending`。オンライン復帰時に自動同期 |
| ペイウォール記事 | Extension が取得した DOM を優先利用。取得不可なら `extraction_failed` として要約なしで保存 |
| PDF / 動画 URL | PDF はテキスト抽出、YouTube は字幕取得（v1.1 で対応、v1.0 は「未対応形式」表示） |
| 重複 URL | 正規化 URL で照合し「既に保存済み」と表示、既存 Item を開く |
| 診断タイムアウト | 60 秒で打ち切り、部分結果を保存し `partial` ステータス |

---

## 4. 機能要件

### 4.1 Share Extension（`ShareExtension`）

| ID | 要件 | 優先度 |
|---|---|---|
| SE-01 | `NSExtensionActivationSupportsWebURLWithMaxCount = 1` および `WebPageWithMaxCount = 1` を宣言 | Must |
| SE-02 | `JavaScript Preprocessing File` により Safari の DOM（`document.documentElement.outerHTML`）、`og:*` メタタグ、選択テキストを取得 | Must |
| SE-03 | 選択テキストがある場合はハイライトとして併せて保存 | Should |
| SE-04 | メモリ上限（Extension は約 120MB）を考慮し HTML は 2MB で切り詰め | Must |
| SE-05 | App Group（`group.com.example.veritas`）共有コンテナへ書き込み | Must |
| SE-06 | 保存完了トースト表示後 0.8 秒で自動 dismiss | Should |
| SE-07 | 他アプリ（X、Chrome、メール、メッセージ）からの URL 共有にも対応 | Must |

#### JavaScript Preprocessing の実装例

```javascript
// File: SharePreprocessor.js
var ShareExtensionPreprocessor = function() {};

ShareExtensionPreprocessor.prototype = {
    run: function(args) {
        const meta = {};
        document.querySelectorAll('meta').forEach(function(m) {
            const key = m.getAttribute('property') || m.getAttribute('name');
            if (key) {
                meta[key] = m.getAttribute('content');
            }
        });

        const html = document.documentElement.outerHTML;
        const MAX = 2 * 1024 * 1024;

        args.completionFunction({
            url: document.URL,
            title: document.title,
            meta: meta,
            selection: window.getSelection().toString(),
            html: html.length > MAX ? html.substring(0, MAX) : html
        });
    },

    finalize: function(args) {}
};

var ExtensionPreprocessingJS = new ShareExtensionPreprocessor();
```

#### Extension 側の受け取り（Swift）

```swift
// File: ShareViewController.swift
import UIKit
import UniformTypeIdentifiers

final class ShareViewController: UIViewController {

    private let store = SharedItemStore()

    override func viewDidLoad() {
        super.viewDidLoad()
        loadSharedPayload()
    }

    private func loadSharedPayload() {
        guard
            let extensionItem = extensionContext?.inputItems.first as? NSExtensionItem,
            let provider = extensionItem.attachments?.first
        else {
            completeRequest()
            return
        }

        let propertyListType = UTType.propertyList.identifier

        if provider.hasItemConformingToTypeIdentifier(propertyListType) {
            provider.loadItem(forTypeIdentifier: propertyListType) { [weak self] item, _ in
                guard
                    let dictionary = item as? NSDictionary,
                    let results = dictionary[NSExtensionJavaScriptPreprocessingResultsKey] as? [String: Any]
                else {
                    return
                }
                self?.handle(payload: SharedPayload(rawResults: results))
            }
        } else {
            provider.loadItem(forTypeIdentifier: UTType.url.identifier) { [weak self] item, _ in
                guard let url = item as? URL else { return }
                self?.handle(payload: SharedPayload(url: url))
            }
        }
    }

    private func handle(payload: SharedPayload) {
        do {
            try store.enqueue(payload)
            DispatchQueue.main.async { [weak self] in
                self?.showSuccessToast()
            }
        } catch {
            DispatchQueue.main.async { [weak self] in
                self?.showFailureToast(error)
            }
        }
    }

    private func completeRequest() {
        extensionContext?.completeRequest(returningItems: nil)
    }
}
```

---

### 4.2 本文抽出（Extraction Service）

| ID | 要件 |
|---|---|
| EX-01 | Extension から渡された DOM を第一候補、サーバ側 fetch を第二候補として本文抽出 |
| EX-02 | Readability アルゴリズム（`@mozilla/readability` 相当）+ 独自ルールで本文・見出し構造を抽出 |
| EX-03 | 抽出メタ: タイトル / 著者 / 発行日 / 更新日 / サイト名 / 言語 / 画像 / 正規 URL / 文字数 / 推定読了時間 |
| EX-04 | 発行日は `article:published_time` → JSON-LD `datePublished` → `<time>` → 本文中の日付表現 → URL パス内日付 の優先順で決定 |
| EX-05 | 本文抽出失敗時は「抽出失敗」として原文リンクのみ保持 |
| EX-06 | 本文の SHA-256 ハッシュを保存し、再診断時の変更検知に利用 |
| EX-07 | `robots.txt` と `noarchive` を尊重。ただし個人利用の私的複製範囲としてローカル保存は許容し、公開共有機能では原文リンクのみ提示 |

---

### 4.3 要約機能（Summarization）

| ID | 要件 |
|---|---|
| SM-01 | 3段階の粒度を生成: **TL;DR（1〜2文）** / **キーポイント（3〜7項目）** / **詳細要約（400〜800字）** |
| SM-02 | 記事の主要な**主張**と**根拠**を分離して抽出 |
| SM-03 | 記事内で言及された**数値・固有名詞・日付**を `facts` 配列として構造化抽出 |
| SM-04 | 原文の言語に関わらず、ユーザー設定言語で要約を出力（既定: 日本語） |
| SM-05 | 長文（> 30,000 トークン相当）は章単位に分割し map-reduce 要約 |
| SM-06 | 要約内の各主張に原文の該当箇所（文字オフセット）を紐付け、タップで原文ジャンプ |
| SM-07 | ハイライト部分がある場合は、そこを重点化した要約も生成 |

---

### 4.4 信頼度診断（Credibility Assessment）

#### 4.4.1 スコア構成

総合スコアは 5 カテゴリの重み付き和で算出する。

[ C = \sum_{i=1}^{5} w_i \cdot s_i \quad , \quad \sum w_i = 1 ]

| # | カテゴリ | 重み ( w_i ) | 評価内容 | 判定方式 |
|---|---|---|---|---|
| 1 | 発信元の評価 | 0.25 | ドメインレピュテーション、運営者情報（特商法/About ページ）の有無、TLD、既知の低品質サイトリスト | ルールベース + DB 参照 |
| 2 | 透明性 | 0.20 | 著者名の明記、著者プロフィール、発行日・更新日の明記、修正履歴、問い合わせ先 | ルールベース |
| 3 | 論拠の質 | 0.25 | 出典リンク数と質（一次情報 / 論文 / 公的機関 / 二次まとめ）、引用の具体性、数値の出所明記 | LLM + リンク解析 |
| 4 | 表現の中立性 | 0.15 | 煽動的表現、断定過多、感情語密度、クリックベイト見出し、広告・アフィリエイト密度、AD 表記 | LLM + 辞書 |
| 5 | 外部照合 | 0.15 | Web 検索で同一主張を扱う他ソースを 3〜5 件取得し、一致 / 矛盾 / 未確認を判定 | LLM + 検索 API |

#### 4.4.2 スコア帯とラベル

| スコア | ラベル | 色 | UI 表現 |
|---|---|---|---|
| 85〜100 | 高い信頼性 | 緑 | 一次情報・公的機関中心 |
| 70〜84 | おおむね信頼できる | 黄緑 | 出典あり、軽微な懸念 |
| 50〜69 | 注意が必要 | 黄 | 出典不足 or 偏りあり |
| 30〜49 | 疑わしい | 橙 | 検証困難な主張が多い |
| 0〜29 | 信頼性が低い | 赤 | 誤情報・宣伝性が強い疑い |

#### 4.4.3 警告フラグ（`flags`）

```
no_author              著者不明
no_date                発行日不明
sponsored              広告・PR 記事
affiliate_heavy        アフィリエイトリンク多数
no_citation            出典の提示がない
contradicted           他ソースと矛盾する主張を含む
sensational            扇動的・断定的表現が顕著
ai_generated_suspect   AI 自動生成の疑い
satire                 風刺・パロディサイト
medical_ymyl           医療・健康分野（要専門家確認）
financial_ymyl         金融・投資分野（要専門家確認）
```

#### 4.4.4 設計原則（重要）

- **政治的・思想的な「正しさ」の判定は行わない。** 評価対象は「情報提示の作法」（出典・透明性・検証可能性）に限定する。
- 各スコアには必ず**根拠テキスト**を併記し、ブラックボックス化を避ける。
- 「AI の判定は参考情報であり最終判断はユーザーが行う」旨を診断画面に常時表示する。
- ユーザーが判定に反論できる **「この診断は不正確」フィードバック** を各項目に設置する。

---

### 4.5 情報の新しさ診断（Freshness Assessment）

#### 4.5.1 半減期モデル

記事のトピックを分類し、分野別の情報半減期 ( H ) を用いて時間減衰スコアを算出する。

[ F_{time} = 100 \times 0.5^{\frac{\Delta t}{H}} ]

( \Delta t ) は更新日（なければ発行日）からの経過日数。

| トピック分類 | 半減期 ( H )（日） | 例 |
|---|---|---|
| AI・生成 AI | 120 | モデル比較、API 仕様 |
| ソフトウェア / ライブラリ | 240 | フレームワーク解説、チュートリアル |
| ハードウェア / ガジェット | 365 | スマホレビュー |
| 市況・経済指標 | 60 | 株価、為替、景況感 |
| 法令・制度・税制 | 500 | 補助金、税率、規制 |
| 医療・健康 | 900 | ガイドライン、治療法 |
| 学術・基礎研究 | 1500 | 理論、実験結果 |
| ニュース（速報性） | 30 | 事件、災害、発表 |
| 歴史・古典・普遍的知識 | 減衰なし（100 固定） | 歴史解説、数学 |
| ハウツー（普遍） | 1200 | 料理、文章術 |

#### 4.5.2 補正項

最終スコアは減衰スコアに補正を加える。

[ F = \mathrm{clamp}\left(F_{time} + \sum_j c_j,\ 0,\ 100\right) ]

| 補正 ( c_j ) | 条件 | 値 |
|---|---|---|
| 更新履歴あり | 発行日より新しい更新日が明記 | +10 |
| バージョン言及の陳腐化 | 本文中の製品/API バージョンが現行より 2 世代以上古い | −20 |
| 年号の陳腐化 | 「2023年最新」等、現在年より 2 年以上古い年号を見出しに含む | −15 |
| 廃止・非推奨検出 | 言及技術・制度が廃止/deprecated と判明 | −30 |
| 後続情報の存在 | 同トピックのより新しい信頼できる記事を検出 | −10 |
| 日付不明 | 発行日を特定できない | スコア上限を 50 に制限 |

#### 4.5.3 出力

| 項目 | 内容 |
|---|---|
| `freshness_score` | 0〜100 |
| `staleness_label` | `current` / `aging` / `stale` / `obsolete` / `timeless` / `unknown` |
| `topic_class` | 判定されたトピック分類 |
| `half_life_days` | 適用された半減期 |
| `obsolete_points` | 陳腐化している可能性のある具体的記述（配列） |
| `successors` | 後続情報の候補（タイトル / URL / 発行日 / 差分の要約）最大 3 件 |

#### 4.5.4 陳腐化アラート（バッチ）

- 週次バッチで保存済み Item を再評価
- `stale` → `obsolete` へ遷移した Item をまとめて週次ダイジェスト通知
  - 例: 「保存済み 5 件が古い情報になった可能性があります」

---

### 4.6 ライブラリ・閲覧機能

| ID | 要件 | 優先度 |
|---|---|---|
| LB-01 | 一覧表示（カード / リスト切替）。サムネイル・信頼度バッジ・鮮度バッジ・読了時間を表示 | Must |
| LB-02 | フィルタ: 未読 / 既読 / お気に入り / タグ / 信頼度帯 / 鮮度帯 / 保存期間 | Must |
| LB-03 | ソート: 保存日 / 発行日 / 信頼度 / 鮮度 / 読了時間 | Must |
| LB-04 | 全文検索（本文・要約・メモ・タグ横断）。日本語形態素対応 | Must |
| LB-05 | セマンティック検索（埋め込みベクトルによる意味検索） | Should |
| LB-06 | リーダービュー（フォント・行間・背景色・ダークモード対応） | Must |
| LB-07 | ハイライト・メモ・タグ付け。タグは AI 自動提案 + 手動編集 | Must |
| LB-08 | オフライン閲覧（本文をローカルに保持） | Must |
| LB-09 | 読み上げ（`AVSpeechSynthesizer` による要約 / 本文の音声再生） | Could |
| LB-10 | エクスポート: Markdown / PDF / Notion / Obsidian / Readwise | Should |
| LB-11 | 複数記事の横断要約（選択した 2〜10 件を比較・統合要約） | Could |
| LB-12 | ウィジェット（未読件数、今日読むべき 3 件のレコメンド） | Could |

---

### 4.7 その他機能

| ID | 要件 |
|---|---|
| OT-01 | Action Extension による Safari 拡張（共有シートを開かず 1 タップ保存） |
| OT-02 | Siri / ショートカット対応（`App Intents`）: 「Veritas に保存」 |
| OT-03 | ユニバーサルリンク / カスタム URL スキーム（`veritas://item/{id}`） |
| OT-04 | iCloud（CloudKit）またはサーバ同期による複数デバイス間同期 |
| OT-05 | Handoff による iPhone ↔ iPad ↔ Mac の閲覧位置引き継ぎ |
| OT-06 | 手動再診断ボタン（本文が更新された記事の再評価） |

---

## 5. 画面仕様

### 5.1 画面一覧

| ID | 画面 | 主要要素 |
|---|---|---|
| S-01 | Share Extension シート | タイトル / サムネ / タグ入力 / メモ / 保存ボタン |
| S-02 | ライブラリ（ホーム） | 検索バー / フィルタチップ / Item カードリスト |
| S-03 | Item 詳細 | ヘッダ（タイトル・出典・日付）/ スコアリング / タブ（要約・本文・診断詳細・メモ） |
| S-04 | 信頼度診断詳細 | 総合スコア / 5 カテゴリのレーダーチャート / 各根拠 / フラグ / 外部照合結果 |
| S-05 | 鮮度診断詳細 | 鮮度ゲージ / 半減期グラフ / 陳腐化ポイント / 後続情報カード |
| S-06 | リーダービュー | 本文 / ハイライト / 表示設定 |
| S-07 | 検索結果 | キーワード + セマンティック結果の統合リスト |
| S-08 | タグ管理 | タグ一覧 / 件数 / 統合・リネーム |
| S-09 | 設定 | 出力言語 / 診断の厳しさ / 通知 / 課金 / データ管理 / プライバシー |
| S-10 | オンボーディング | 共有シートへの追加手順アニメーション / 診断の限界に関する説明 |
| S-11 | 週次ダイジェスト | 陳腐化アラート / 未読リマインド / 保存傾向レポート |

### 5.2 Item 詳細画面のスコア表示レイアウト

```
┌──────────────────────────────────────┐
│  ← 戻る                    ⋯ その他   │
├──────────────────────────────────────┤
│  記事タイトルが2行まで表示される       │
│  example.com · 著者名 · 2024/03/12    │
│  約 8 分で読めます                     │
├──────────────────────────────────────┤
│  ┌────────────┐  ┌────────────┐      │
│  │ 信頼度 72  │  │ 鮮度 41    │      │
│  │ おおむね   │  │ 古くなって │      │
│  │ 信頼できる │  │ いる可能性 │      │
│  └────────────┘  └────────────┘      │
│  ⚠ 出典の提示が少ない                 │
│  ⚠ より新しい情報が 2 件見つかりました │
├──────────────────────────────────────┤
│  [ 要約 ] [ 本文 ] [ 診断 ] [ メモ ]  │
├──────────────────────────────────────┤
│  TL;DR                                │
│  ・…                                   │
│                                        │
│  キーポイント                          │
│  1. …                                  │
└──────────────────────────────────────┘
```

---

## 6. システムアーキテクチャ

### 6.1 構成図

```
[iPhone]
 ├ Main App (SwiftUI)
 │   ├ SwiftData (ローカル DB)
 │   ├ Sync Engine
 │   └ BGTaskScheduler (アップロードキュー)
 ├ Share Extension ─┐
 └ Action Extension ┤→ App Group Container (共有 SQLite / JSON キュー)
                    │
                    ▼
             [API Gateway] (HTTPS / OIDC)
                    │
        ┌───────────┴────────────┐
        ▼                        ▼
 [API Server]              [Job Queue]
 Node.js / Fastify          Redis + BullMQ
        │                        │
        │                        ▼
        │              [Worker Pool]
        │               ├ Extraction Worker
        │               ├ Summarize Worker
        │               ├ Credibility Worker
        │               └ Freshness Worker
        │                        │
        ▼                        ▼
 [PostgreSQL + pgvector]  [外部サービス]
 [Object Storage (S3)]     ├ LLM API
 [OpenSearch (全文検索)]    ├ Web Search API
                           └ Domain Reputation API
```

### 6.2 処理パイプライン（サーバ）

```
ingest
  → normalize_url
  → dedupe_check
  → extract (DOM優先 / fetch fallback)
  → classify_topic          ┐
  → summarize               ├ 並列実行
  → credibility_analyze     │
  → freshness_analyze       ┘
  → corroborate (Web検索 → 照合)
  → embed (ベクトル化)
  → persist
  → push_notification
```

- 並列 3 ジョブの合計 SLA: p50 12 秒 / p95 35 秒 / タイムアウト 60 秒
- 各ステップは冪等。失敗時は指数バックオフで最大 3 回リトライ

---

## 7. データモデル

### 7.1 主要テーブル（PostgreSQL）

```sql
-- File: schema.sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    apple_sub       TEXT UNIQUE NOT NULL,
    email           TEXT,
    plan            TEXT NOT NULL DEFAULT 'free',
    output_language TEXT NOT NULL DEFAULT 'ja',
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE items (
    id                 UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id            UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    original_url       TEXT NOT NULL,
    canonical_url      TEXT NOT NULL,
    url_hash           TEXT NOT NULL,
    title              TEXT,
    site_name          TEXT,
    author             TEXT,
    published_at       TIMESTAMPTZ,
    updated_at_source  TIMESTAMPTZ,
    language           TEXT,
    hero_image_url     TEXT,
    char_count         INTEGER,
    reading_minutes    INTEGER,
    content_hash       TEXT,
    body_storage_key   TEXT,
    status             TEXT NOT NULL DEFAULT 'pending',
    is_read            BOOLEAN NOT NULL DEFAULT false,
    is_favorite        BOOLEAN NOT NULL DEFAULT false,
    saved_at           TIMESTAMPTZ NOT NULL DEFAULT now(),
    analyzed_at        TIMESTAMPTZ,
    UNIQUE (user_id, url_hash)
);

CREATE TABLE summaries (
    item_id       UUID PRIMARY KEY REFERENCES items(id) ON DELETE CASCADE,
    tldr          TEXT NOT NULL,
    key_points    JSONB NOT NULL,
    detailed      TEXT,
    claims        JSONB,
    facts         JSONB,
    model         TEXT NOT NULL,
    language      TEXT NOT NULL,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE credibility_reports (
    item_id           UUID PRIMARY KEY REFERENCES items(id) ON DELETE CASCADE,
    total_score       SMALLINT NOT NULL,
    label             TEXT NOT NULL,
    source_score      SMALLINT,
    transparency_score SMALLINT,
    evidence_score    SMALLINT,
    neutrality_score  SMALLINT,
    corroboration_score SMALLINT,
    rationale         JSONB NOT NULL,
    flags             TEXT[] NOT NULL DEFAULT '{}',
    corroborations    JSONB,
    model             TEXT NOT NULL,
    created_at        TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE freshness_reports (
    item_id         UUID PRIMARY KEY REFERENCES items(id) ON DELETE CASCADE,
    freshness_score SMALLINT NOT NULL,
    staleness_label TEXT NOT NULL,
    topic_class     TEXT NOT NULL,
    half_life_days  INTEGER NOT NULL,
    elapsed_days    INTEGER,
    adjustments     JSONB NOT NULL,
    obsolete_points JSONB,
    successors      JSONB,
    evaluated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE tags (
    id       UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id  UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name     TEXT NOT NULL,
    UNIQUE (user_id, name)
);

CREATE TABLE item_tags (
    item_id UUID NOT NULL REFERENCES items(id) ON DELETE CASCADE,
    tag_id  UUID NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
    source  TEXT NOT NULL DEFAULT 'ai',
    PRIMARY KEY (item_id, tag_id)
);

CREATE TABLE highlights (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    item_id     UUID NOT NULL REFERENCES items(id) ON DELETE CASCADE,
    quote       TEXT NOT NULL,
    start_offset INTEGER,
    end_offset   INTEGER,
    note        TEXT,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE item_embeddings (
    item_id   UUID PRIMARY KEY REFERENCES items(id) ON DELETE CASCADE,
    embedding VECTOR(1536) NOT NULL
);

CREATE TABLE domain_reputations (
    domain          TEXT PRIMARY KEY,
    base_score      SMALLINT NOT NULL,
    category        TEXT,
    has_editorial_policy BOOLEAN,
    notes           TEXT,
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE analysis_feedbacks (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    item_id    UUID NOT NULL REFERENCES items(id) ON DELETE CASCADE,
    user_id    UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    target     TEXT NOT NULL,
    verdict    TEXT NOT NULL,
    comment    TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_items_user_saved ON items (user_id, saved_at DESC);
CREATE INDEX idx_items_status ON items (status) WHERE status <> 'completed';
CREATE INDEX idx_embeddings_hnsw ON item_embeddings
    USING hnsw (embedding vector_cosine_ops);
```

### 7.2 `items.status` の状態遷移

```
pending → extracting → analyzing → completed
                    ↘ extraction_failed
                    ↘ partial
                    ↘ unsupported_format
completed → reanalyzing → completed
```

### 7.3 クライアント側モデル（SwiftData）

```swift
// File: Item.swift
import Foundation
import SwiftData

@Model
final class Item {
    @Attribute(.unique) var id: UUID
    var canonicalURL: String
    var title: String
    var siteName: String?
    var author: String?
    var publishedAt: Date?
    var savedAt: Date
    var readingMinutes: Int
    var bodyMarkdown: String?
    var status: ItemStatus
    var isRead: Bool
    var isFavorite: Bool

    var credibilityScore: Int?
    var credibilityLabel: String?
    var freshnessScore: Int?
    var stalenessLabel: String?
    var flags: [String]

    @Relationship(deleteRule: .cascade) var highlights: [Highlight]
    @Relationship(inverse: \Tag.items) var tags: [Tag]

    init(id: UUID = UUID(), canonicalURL: String, title: String) {
        self.id = id
        self.canonicalURL = canonicalURL
        self.title = title
        self.savedAt = .now
        self.readingMinutes = 0
        self.status = .pending
        self.isRead = false
        self.isFavorite = false
        self.flags = []
        self.highlights = []
        self.tags = []
    }
}

enum ItemStatus: String, Codable {
    case pending
    case extracting
    case analyzing
    case completed
    case partial
    case extractionFailed
    case unsupportedFormat
}
```

---

## 8. API 仕様

### 8.1 エンドポイント一覧

| Method | Path | 説明 |
|---|---|---|
| `POST` | `/v1/auth/apple` | Sign in with Apple のトークン交換 |
| `POST` | `/v1/items` | 記事の保存（診断ジョブをキューイング） |
| `POST` | `/v1/items/batch` | オフライン蓄積分の一括同期（最大 50 件） |
| `GET` | `/v1/items` | 一覧取得（カーソルページング / フィルタ / ソート） |
| `GET` | `/v1/items/{id}` | 詳細取得（要約・診断を含む） |
| `PATCH` | `/v1/items/{id}` | 既読 / お気に入り / タグ / メモの更新 |
| `DELETE` | `/v1/items/{id}` | 削除 |
| `POST` | `/v1/items/{id}/reanalyze` | 再診断の実行 |
| `GET` | `/v1/items/{id}/body` | 本文取得（署名付き URL） |
| `GET` | `/v1/search` | キーワード + セマンティック検索 |
| `POST` | `/v1/digest/weekly` | 週次ダイジェスト取得 |
| `POST` | `/v1/feedback` | 診断結果へのフィードバック送信 |
| `GET` | `/v1/sync?since=` | 差分同期 |
| `DELETE` | `/v1/account` | アカウントと全データの削除 |

### 8.2 `POST /v1/items` リクエスト

```json
{
  "client_item_id": "9f0b3f1e-2c4a-4a10-9f9b-6a0f0f6e6a11",
  "url": "https://example.com/articles/ai-trend-2024",
  "shared_at": "2026-08-28T09:12:44+09:00",
  "source_app": "com.apple.mobilesafari",
  "page": {
    "title": "2024年のAIトレンド総まとめ",
    "html": "<!DOCTYPE html>...",
    "meta": {
      "og:title": "2024年のAIトレンド総まとめ",
      "article:published_time": "2024-03-12T00:00:00Z",
      "author": "山田太郎"
    },
    "selection": "推論コストは前年比で約80%低下した"
  },
  "user_tags": ["AI", "調査"],
  "user_note": "社内共有用"
}
```

### 8.3 `GET /v1/items/{id}` レスポンス

```json
{
  "id": "3d2c1b0a-1111-2222-3333-444455556666",
  "status": "completed",
  "meta": {
    "canonical_url": "https://example.com/articles/ai-trend-2024",
    "title": "2024年のAIトレンド総まとめ",
    "site_name": "Example Tech",
    "author": "山田太郎",
    "published_at": "2024-03-12T00:00:00Z",
    "updated_at_source": null,
    "language": "ja",
    "char_count": 8420,
    "reading_minutes": 8
  },
  "summary": {
    "tldr": "2024年のAI業界は推論コストの急落とオープンモデルの台頭が中心で、実運用フェーズへの移行が進んだ。",
    "key_points": [
      "推論コストが前年比約80%低下したと主張",
      "オープンウェイトモデルが商用利用で拡大",
      "規制面ではEU AI Actの段階施行が始まった"
    ],
    "detailed": "...",
    "claims": [
      {
        "claim": "推論コストは前年比で約80%低下した",
        "evidence": "特定ベンダーの価格表を根拠として提示",
        "source_offset": [1820, 1904],
        "evidence_quality": "single_vendor"
      }
    ],
    "facts": [
      { "type": "percentage", "value": "80%", "context": "推論コスト低下率" },
      { "type": "regulation", "value": "EU AI Act", "context": "段階施行" }
    ]
  },
  "credibility": {
    "total_score": 72,
    "label": "おおむね信頼できる",
    "breakdown": {
      "source": 78,
      "transparency": 85,
      "evidence": 58,
      "neutrality": 74,
      "corroboration": 66
    },
    "rationale": {
      "source": "運営者情報と編集方針のページが確認できる技術メディア。",
      "transparency": "著者名・発行日が明記されている。更新履歴は未記載。",
      "evidence": "外部リンク6件のうち一次情報は1件のみ。主要な数値の出所が単一ベンダー。",
      "neutrality": "断定的表現がやや多いが、扇動的表現は限定的。",
      "corroboration": "他3件のソースで同傾向の記述を確認。低下率の数値は幅がある。"
    },
    "flags": ["no_citation"],
    "corroborations": [
      {
        "title": "推論コスト動向レポート",
        "url": "https://example.org/report",
        "published_at": "2024-05-02",
        "verdict": "supports",
        "note": "低下率は60〜75%と報告"
      }
    ]
  },
  "freshness": {
    "freshness_score": 41,
    "staleness_label": "stale",
    "topic_class": "ai",
    "half_life_days": 120,
    "elapsed_days": 899,
    "adjustments": [
      { "reason": "successor_exists", "delta": -10 },
      { "reason": "version_outdated", "delta": -20 }
    ],
    "obsolete_points": [
      {
        "quote": "現時点で最も安価なAPIは……",
        "reason": "記事公開後に複数の値下げと新モデル投入があり、価格前提が変化している可能性が高い"
      }
    ],
    "successors": [
      {
        "title": "2026年版 AI推論コスト比較",
        "url": "https://example.com/articles/ai-cost-2026",
        "published_at": "2026-04-18",
        "diff_summary": "モデル世代が2世代進み、価格体系がトークン課金から従量+バッチ割引に変化"
      }
    ]
  },
  "tags": ["AI", "調査", "業界動向"],
  "is_read": false,
  "is_favorite": false,
  "saved_at": "2026-08-28T09:12:44+09:00",
  "analyzed_at": "2026-08-28T09:13:02+09:00"
}
```

### 8.4 共通エラー形式

```json
{
  "error": {
    "code": "quota_exceeded",
    "message": "今月の診断回数の上限に達しました。",
    "retry_after": 0,
    "docs_url": "https://veritas.example.com/docs/errors#quota_exceeded"
  }
}
```

| コード | HTTP | 説明 |
|---|---|---|
| `invalid_url` | 400 | URL 形式が不正 |
| `unauthorized` | 401 | トークン無効 |
| `duplicate_item` | 409 | 同一 URL が既に存在（既存 ID を返す） |
| `quota_exceeded` | 429 | 診断上限超過 |
| `extraction_failed` | 422 | 本文抽出不可 |
| `upstream_timeout` | 504 | LLM / 検索 API のタイムアウト |

---

## 9. AI 実装仕様

### 9.1 モデル選定方針

| 用途 | モデル階層 | 理由 |
|---|---|---|
| トピック分類 | 小型・高速モデル | 分類のみ。コスト最小化 |
| 要約（短文） | 小型〜中型モデル | 十分な品質 |
| 要約（長文） | 中型モデル + map-reduce | コストと品質の均衡 |
| 信頼度診断 | 大型モデル（推論強化） | 多段の判断が必要 |
| 陳腐化判定 | 中型 + Web 検索ツール | 外部知識が必須 |
| 埋め込み | 埋め込み専用モデル（1536 次元） | 検索用 |

- **プロバイダ抽象化レイヤ**を設け、モデル差し替えを設定変更のみで可能にする
- すべての診断出力は **JSON Schema による Structured Output** で強制
- `temperature = 0.2`、`seed` 固定で再現性を確保

### 9.2 信頼度診断プロンプト（抜粋）

```text
あなたは情報リテラシー教育の専門家です。与えられた記事について、
「主張内容の政治的・思想的な正しさ」ではなく、
「情報提示の作法（検証可能性・透明性・出典の質）」のみを評価してください。

## 絶対に守るルール
1. 政治的立場、宗教、思想の是非を評価対象にしない。
2. 評価は必ず記事本文とメタデータから引用できる事実に基づく。
3. 判断できない項目は推測せず confidence を "low" とする。
4. 各スコアには必ず本文からの引用（60字以内）を根拠として添える。
5. 医療・金融・法律に関する記事は該当フラグを立て、専門家確認を促す。

## 入力
- URL: {{url}}
- ドメイン評価（既知情報）: {{domain_reputation}}
- 著者: {{author}}
- 発行日: {{published_at}} / 更新日: {{updated_at}}
- 外部リンク一覧: {{outbound_links}}
- 広告・アフィリエイトリンク数: {{ad_link_count}}
- 本文: {{body}}

## 出力
credibility_report スキーマに厳密に従った JSON のみを出力する。
```

### 9.3 信頼度診断の出力スキーマ

```json
{
  "type": "object",
  "required": ["scores", "rationale", "flags", "confidence"],
  "properties": {
    "scores": {
      "type": "object",
      "required": ["source", "transparency", "evidence", "neutrality"],
      "properties": {
        "source": { "type": "integer", "minimum": 0, "maximum": 100 },
        "transparency": { "type": "integer", "minimum": 0, "maximum": 100 },
        "evidence": { "type": "integer", "minimum": 0, "maximum": 100 },
        "neutrality": { "type": "integer", "minimum": 0, "maximum": 100 }
      }
    },
    "rationale": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["reason", "quote"],
        "properties": {
          "reason": { "type": "string", "maxLength": 200 },
          "quote": { "type": "string", "maxLength": 120 }
        }
      }
    },
    "flags": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "no_author", "no_date", "sponsored", "affiliate_heavy",
          "no_citation", "contradicted", "sensational",
          "ai_generated_suspect", "satire", "medical_ymyl", "financial_ymyl"
        ]
      }
    },
    "confidence": { "type": "string", "enum": ["high", "medium", "low"] }
  }
}
```

### 9.4 スコア合成の実装

```typescript
// File: src/scoring/credibility.ts
export interface CategoryScores {
  source: number;
  transparency: number;
  evidence: number;
  neutrality: number;
  corroboration: number;
}

const WEIGHTS: CategoryScores = {
  source: 0.25,
  transparency: 0.20,
  evidence: 0.25,
  neutrality: 0.15,
  corroboration: 0.15,
};

const FLAG_PENALTIES: Record<string, number> = {
  sponsored: 8,
  affiliate_heavy: 10,
  no_citation: 6,
  contradicted: 18,
  sensational: 8,
  ai_generated_suspect: 10,
  satire: 25,
};

export function composeCredibilityScore(
  scores: CategoryScores,
  flags: string[],
  confidence: 'high' | 'medium' | 'low',
): { total: number; label: string } {
  const weighted = (Object.keys(WEIGHTS) as (keyof CategoryScores)[]).reduce(
    (accumulator, key) => accumulator + scores[key] * WEIGHTS[key],
    0,
  );

  const penalty = flags.reduce(
    (accumulator, flag) => accumulator + (FLAG_PENALTIES[flag] ?? 0),
    0,
  );

  // 確信度が低い場合は中央（50）方向へ収縮させ、断定を避ける
  const shrinkFactor = confidence === 'low' ? 0.6 : confidence === 'medium' ? 0.85 : 1.0;
  const shrunk = 50 + (weighted - penalty - 50) * shrinkFactor;

  const total = Math.max(0, Math.min(100, Math.round(shrunk)));

  return { total, label: toLabel(total) };
}

function toLabel(total: number): string {
  if (total >= 85) return '高い信頼性';
  if (total >= 70) return 'おおむね信頼できる';
  if (total >= 50) return '注意が必要';
  if (total >= 30) return '疑わしい';
  return '信頼性が低い';
}
```

### 9.5 鮮度スコアの実装

```typescript
// File: src/scoring/freshness.ts
const HALF_LIFE_DAYS: Record<string, number | null> = {
  ai: 120,
  software: 240,
  hardware: 365,
  market: 60,
  regulation: 500,
  medical: 900,
  academic: 1500,
  news: 30,
  howto: 1200,
  timeless: null,
};

export interface Adjustment {
  reason: string;
  delta: number;
}

export function computeFreshness(params: {
  topicClass: string;
  publishedAt: Date | null;
  updatedAt: Date | null;
  adjustments: Adjustment[];
  now?: Date;
}): { score: number; label: string; elapsedDays: number | null; halfLife: number | null } {
  const { topicClass, publishedAt, updatedAt, adjustments } = params;
  const now = params.now ?? new Date();
  const halfLife = HALF_LIFE_DAYS[topicClass] ?? 240;

  if (halfLife === null) {
    return { score: 100, label: 'timeless', elapsedDays: null, halfLife: null };
  }

  const baseDate = updatedAt ?? publishedAt;

  if (baseDate === null) {
    const unknownScore = Math.max(0, Math.min(50, 50 + sumDelta(adjustments)));
    return { score: unknownScore, label: 'unknown', elapsedDays: null, halfLife };
  }

  const elapsedDays = Math.floor(
    (now.getTime() - baseDate.getTime()) / (1000 * 60 * 60 * 24),
  );

  const decayed = 100 * Math.pow(0.5, elapsedDays / halfLife);
  const score = Math.max(0, Math.min(100, Math.round(decayed + sumDelta(adjustments))));

  return { score, label: toStalenessLabel(score), elapsedDays, halfLife };
}

function sumDelta(adjustments: Adjustment[]): number {
  return adjustments.reduce((accumulator, item) => accumulator + item.delta, 0);
}

function toStalenessLabel(score: number): string {
  if (score >= 75) return 'current';
  if (score >= 45) return 'aging';
  if (score >= 20) return 'stale';
  return 'obsolete';
}
```

### 9.6 ハルシネーション対策

| 対策 | 内容 |
|---|---|
| 引用強制 | 各根拠に本文からの引用を必須化し、原文と文字列照合（不一致なら再生成） |
| Grounding 検証 | 要約内の数値・固有名詞が原文に存在するか正規表現で機械検証 |
| 二段検証 | 信頼度スコアが 30 未満 / 90 超の極端値のみ再評価を実行 |
| 出典の実在確認 | `successors` / `corroborations` の URL は HTTP ステータスを確認してから提示 |
| 不明の明示 | 判定不能項目は「判定できませんでした」と表示し、スコア計算から除外して重み再正規化 |

---

## 10. 非機能要件

### 10.1 パフォーマンス

| 項目 | 目標値 |
|---|---|
| Share Extension 起動 → UI 表示 | 300ms 以内 |
| 保存タップ → シート消滅 | 800ms 以内 |
| アプリ起動 → ライブラリ表示 | 1.0 秒以内（cold start） |
| 診断完了（p50 / p95） | 12 秒 / 35 秒 |
| ライブラリスクロール | 60fps 維持（1,000 件時） |
| 全文検索レスポンス | 300ms 以内 |

### 10.2 信頼性・可用性

- API 稼働率 99.5% 以上
- ジョブの at-least-once 保証 + 冪等キーによる重複排除
- LLM プロバイダ障害時は代替プロバイダへ自動フェイルオーバー
- 保存操作は常にローカル成功を優先（サーバ障害でも保存は失敗しない）

### 10.3 セキュリティ

| 項目 | 内容 |
|---|---|
| 認証 | Sign in with Apple（OIDC）。アクセストークン 1 時間 / リフレッシュトークン 90 日 |
| トークン保管 | Keychain（`kSecAttrAccessibleAfterFirstUnlock`、App Group 共有） |
| 通信 | TLS 1.3 必須、ATS 有効、証明書ピンニング |
| 保存時暗号化 | DB は AES-256 で暗号化。本文はオブジェクトストレージで SSE-KMS |
| ローカル | SwiftData の保存先を暗号化。生体認証によるアプリロック（任意） |
| ログ | URL と本文はマスキング。診断ログは 30 日で自動削除 |
| LLM 送信 | プロバイダの学習利用オプトアウト契約を必須。ゼロデータリテンション設定 |

### 10.4 プライバシー

- **Privacy Manifest（`PrivacyInfo.xcprivacy`）** を同梱し、収集データを明示
- App Store の Privacy Nutrition Label 申告項目:
  - 「ブラウズ履歴（保存した URL）」— アプリ機能のため / ユーザーに紐付く
  - 「ユーザーコンテンツ（メモ・ハイライト）」— アプリ機能のため / ユーザーに紐付く
  - トラッキング目的の利用: **なし**
- 「サーバ送信せずオンデバイス処理のみで使う」モードを設定に用意（v1.1、`Foundation Models` 等のオンデバイス LLM 利用。要約のみ対応）
- アカウント削除で全データを 30 日以内に完全消去（App Store 要件）

### 10.5 アクセシビリティ

- VoiceOver 完全対応。スコアは「信頼度 72 点、おおむね信頼できる」と読み上げ
- Dynamic Type 対応（最大アクセシビリティサイズまで崩れない）
- スコアの色分けに加え、必ずアイコン形状とテキストラベルを併記（色覚多様性への配慮）
- コントラスト比 4.5:1 以上

### 10.6 法務・コンプライアンス

| 論点 | 方針 |
|---|---|
| 著作権 | 記事本文の保存は**私的複製の範囲に限定**。他ユーザーへの本文共有機能は実装しない。共有は URL + 自作の要約のみ |
| 引用 | 診断根拠の引用は必要最小限（1 箇所 120 字以内）に制限 |
| 名誉毀損リスク | 「このサイトはフェイクニュースである」等の断定表現を出力しない。評価は「出典の提示が少ない」等の記述的表現に限定 |
| 免責 | 利用規約・診断画面に「AI 判定は参考情報であり正確性を保証しない」旨を明記 |
| YMYL 分野 | 医療・金融・法律の記事には専門家への相談を促す固定文言を表示 |
| `robots.txt` | サーバ側 fetch では遵守。`noarchive` 指定サイトは本文をサーバ保存せずクライアントのみ保持 |

---

## 11. 技術スタック

### 11.1 クライアント

| 領域 | 採用技術 |
|---|---|
| UI | SwiftUI（iOS 17+） |
| アーキテクチャ | MVVM + Repository、`@Observable` |
| 永続化 | SwiftData（SQLite バックエンド） |
| 拡張 | Share Extension、Action Extension、App Intents、WidgetKit |
| ネットワーク | `URLSession` + async/await、`BGTaskScheduler` |
| 本文表示 | `AttributedString` レンダリング（WKWebView は使わずネイティブ描画） |
| テスト | Swift Testing、XCUITest、スナップショットテスト |

### 11.2 サーバ

| 領域 | 採用技術 |
|---|---|
| API | Node.js 22 + Fastify + TypeScript |
| ジョブ | Redis + BullMQ（Worker は水平スケール） |
| DB | PostgreSQL 16 + pgvector |
| 全文検索 | OpenSearch（日本語は Sudachi アナライザ） |
| ストレージ | S3 互換オブジェクトストレージ |
| 抽出 | `@mozilla/readability` + `jsdom` + `cheerio` + Playwright（JS 必須サイト用） |
| 監視 | OpenTelemetry + Grafana / Loki、Sentry |
| CI/CD | GitHub Actions → コンテナデプロイ（ステージング / 本番） |
| IaC | Terraform |

---

## 12. コスト設計と課金

### 12.1 1 記事あたりの想定コスト

| 処理 | 想定 | 概算 |
|---|---|---|
| 抽出 | 自前処理 | ほぼ 0 |
| トピック分類 | 小型モデル 2K トークン | 極小 |
| 要約 | 中型モデル 10K in / 1.5K out | 中 |
| 信頼度診断 | 大型モデル 12K in / 2K out | 大 |
| 外部照合 | 検索 API 1〜2 コール + 判定 | 中 |
| 鮮度診断 | 中型モデル + 検索 1 コール | 中 |
| 埋め込み | 埋め込みモデル | 極小 |

> **見積り目安**: 1 記事あたり 15〜40 円程度。無料枠設計はこのコストを前提とする。

### 12.2 コスト最適化

- **段階的診断**: 保存時は「抽出 + 要約」のみ実行。信頼度・鮮度の詳細診断は **Item を開いた時に初回実行**（未読記事への課金を回避）
- 同一 URL は全ユーザー間で診断結果をキャッシュ共有（本文・メモは個人ごとに分離）
- プロンプトキャッシュの活用
- 短文記事（< 500 字）は要約をスキップし全文表示
- 本文ハッシュ一致時は再診断をスキップ

### 12.3 料金プラン

| プラン | 月額 | 保存数 | AI 診断 | その他 |
|---|---|---|---|---|
| Free | 0 円 | 無制限 | 月 10 記事 | 広告なし、要約のみ |
| Plus | 600 円 | 無制限 | 月 200 記事 | 全診断、セマンティック検索、エクスポート |
| Pro | 1,500 円 | 無制限 | 月 1,000 記事 | 横断要約、週次ダイジェスト、API アクセス |

- 実装: StoreKit 2 による自動更新サブスクリプション
- 無料枠超過時は「要約のみ」へ自動フォールバックし、機能を完全停止しない

---

## 13. 開発計画

| フェーズ | 期間目安 | スコープ |
|---|---|---|
| Phase 0: 設計・PoC | 2 週 | 抽出精度検証、診断プロンプトの評価データセット作成（100 記事） |
| Phase 1: MVP | 6 週 | Share Extension、保存、抽出、要約、ライブラリ、リーダービュー |
| Phase 2: 診断 | 5 週 | 信頼度診断、鮮度診断、診断詳細画面、フィードバック機能 |
| Phase 3: 拡張 | 4 週 | 検索（全文 + セマンティック）、タグ、エクスポート、課金 |
| Phase 4: 品質 | 3 週 | 陳腐化バッチ、ウィジェット、アクセシビリティ、パフォーマンス改善 |
| Phase 5: リリース | 2 週 | ベータ（TestFlight）、審査対応、公開 |

### 13.1 品質評価の基準（Phase 0 で確立）

| 指標 | 目標 |
|---|---|
| 本文抽出成功率 | 主要 200 サイトで 95% 以上 |
| 発行日抽出精度 | 90% 以上 |
| 要約の事実整合率（人手評価） | 95% 以上（原文に無い記述を含まない） |
| 信頼度スコアの人手評価との相関 | Spearman ( \rho \geq 0.7 ) |
| トピック分類精度 | 85% 以上 |

---

## 14. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| 誤った信頼度判定による名誉毀損・炎上 | 高 | 断定表現の禁止、根拠必須化、フィードバック導線、専門家レビューによるプロンプト監査 |
| 著作権侵害の指摘 | 高 | 本文の対外共有機能を実装しない、`noarchive` 尊重、引用の字数制限 |
| LLM コストの想定超過 | 高 | 段階的診断、結果キャッシュ共有、プラン別上限、コストアラート |
| Extension のメモリ超過によるクラッシュ | 中 | HTML を 2MB で切り詰め、重い処理は本体アプリへ委譲 |
| App Store 審査でのリジェクト | 中 | アカウント削除機能、Privacy Manifest、AI 免責表示、サブスク表記要件を事前充足 |
| ペイウォールサイトの抽出不可 | 中 | Extension の DOM 優先取得で大半をカバー、失敗時は明示的に通知 |
| モデル更新による診断結果の非互換 | 中 | 診断結果にモデル名・プロンプトバージョンを記録、変更時は再診断を提案 |
| 診断結果への過度な依存 | 中 | オンボーディングで限界を明示、スコアと併せて必ず根拠を提示 |

---

## 15. 将来拡張（v2.0 以降）

| 機能 | 概要 |
|---|---|
| ナレッジグラフ | 保存記事間の関連を可視化し、テーマごとのクラスタを自動生成 |
| RAG チャット | 保存記事全体に対する対話型質問応答（出典付き回答） |
| 主張の時系列追跡 | 同一トピックの主張がどう変化したかをタイムライン表示 |
| バイアス多角化 | 同テーマの異なる論調の記事を推薦し、視点の偏りを是正 |
| チーム共有 | ワークスペース単位での記事共有（URL + 要約のみ） |
| Mac / Vision アプリ | SwiftUI 共通化によるマルチプラットフォーム展開 |
| Android / Web | サーバ API の再利用による展開 |
| 音声要約ポッドキャスト | 週次の保存記事を音声番組として自動生成 |

---

## 16. 付録

### 16.1 `Info.plist` 設定（Share Extension）

```xml
<!-- File: ShareExtension/Info.plist -->
<key>NSExtension</key>
<dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.share-services</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
    <key>NSExtensionAttributes</key>
    <dict>
        <key>NSExtensionActivationRule</key>
        <dict>
            <key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
            <integer>1</integer>
            <key>NSExtensionActivationSupportsWebPageWithMaxCount</key>
            <integer>1</integer>
            <key>NSExtensionActivationSupportsText</key>
            <true/>
        </dict>
        <key>NSExtensionJavaScriptPreprocessingFile</key>
        <string>SharePreprocessor</string>
    </dict>
</dict>
```

### 16.2 URL 正規化ルール

1. スキームを `https` に統一（`http` も `https` として扱う）
2. ホスト名を小文字化し、先頭の `www.` を除去
3. トラッキングパラメータを除去: `utm_*`, `fbclid`, `gclid`, `yclid`, `ref`, `ref_src`, `spm`, `at_medium`, `at_campaign`
4. フラグメント（`#`）を除去（ただし SPA のハッシュルーティングは保持）
5. 末尾スラッシュを除去
6. `<link rel="canonical">` が存在する場合はそちらを優先
7. AMP URL（`/amp/`, `?amp=1`）は正規版へ変換
8. 正規化後の URL を SHA-256 でハッシュ化し `url_hash` として重複判定に使用

### 16.3 診断画面の必須表示文言

```
この診断は AI による自動評価であり、正確性を保証するものではありません。
評価対象は情報の提示方法（出典・透明性・検証可能性）であり、
主張内容の正しさや思想的な立場を判定するものではありません。
最終的な判断はご自身で行ってください。
```